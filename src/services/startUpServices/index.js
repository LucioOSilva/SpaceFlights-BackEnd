const { seedDataBaseRoutine } = require('./articlesSeedService');

module.exports = { seedDataBaseRoutine };

/*
  Caro avaliador - neste projeto optei por implementar manualmente o cron,
  compreendo o uso e a utilidade sem contar no baixo nível de complexidade para
  implementar a lib "node-cron", mas o fiz por diversão além de um desafio a mais.
*/
